build:
  image: default-jammy

  environment:
    node: 18

  nodes:
    coverage:
      tests:
        override:
          - npm ci
          - npm test
          - command: npm run coverage
            coverage:
              file: coverage/lcov.info
              format: lcov
          - js-scrutinizer-run
